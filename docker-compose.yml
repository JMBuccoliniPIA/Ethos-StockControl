version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: ethos-stock-mongo
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: ethos-stock

volumes:
  mongo-data:
